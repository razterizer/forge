# Executable example

This example builds and runs a small multi-file executable.

From this directory:

```sh
../../build/dev/forge build
../../build/dev/forge run
../../build/dev/forge release
```

The local release archive is written under `.forge/release/`.
